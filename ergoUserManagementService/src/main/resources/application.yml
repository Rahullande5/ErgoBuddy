server:
  port: 9187

spring:
  application:
    name: ergoUserManagementService

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ergoUserManagement
    username: postgres
    password: abcd@1234

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
